<!DOCTYPE html>
<html>
  <head>
    <title>Volunteer Connect</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js"></script>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <script src="js/jquery-3.2.1.min.js"></script>
    <script src="js/fontawesome-all.min.js"></script>
    <style>
        a:link { text-decoration: none; }


        a:visited { text-decoration: none; }


        a:hover { text-decoration: none; }


        a:active { text-decoration: none; }
    </style>
    <link href="https://use.fontawesome.com/releases/v5.0.4/css/all.css" rel="stylesheet">
  </head>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <button type="button" class="btn">
          <li id = home_button class = "nav-item"><%= link_to 'Home', home_url %></li>
      </button>
        
      <button type="button" class="btn">
          <td class = "nav-item"><%= link_to 'Organizations', organizations_url %></td>
      </button>
        
      <button type="button" class="btn">
          <td class = "nav-item"><%= link_to 'Events', events_url %></td>
      </button>
       
       <% if signed_in? %>
        <button type="button" class="btn">
          <a class="nav-item"><%= link_to "Profile", users_show_path %></a>
      </button>
        
      <button type="button" class="btn">
          <a class="nav-item"><%= link_to "Logout", destroy_user_session_path, method: :delete %></a>
      </button>
        <% else %>
      <button type="button" class="btn">
          <td class="nav-item"><%= link_to "Sign Up", new_user_registration_path %></td>
      </button>
        
      <button type="button" class="btn">
          <a class="nav-item"><%= link_to "Login", new_user_session_path %></a>
      </button>

        <% end %>
        
      
       
        
       <% if signed_in? %> 
        <%= "Hello #{current_user.email}" %>
        <% end %>
      
        
        


    </div>
  </div>
</nav>

<%= form_tag(search_path, :method => "get") do %>
    <div class="input-group my-2 my-lg-0">
        <%= search_field_tag :search, params[:search], placeholder: "Search For Organizations or Events", class: "form-control my-2 my-lg-0"%>
        <div class="input-group-btn-outline-success my-2 my-sm-0">
            <%= button_tag "Search", :class => 'btn btn-into glyphicon glyphicon-search', :name => nil%>
        </div>
    </div>
<% end %>
  
  <body>
    <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
    <p class = "alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
  <div>
    
  </div>
  <div id="footer ">
  <!-- Grid container -->
  <div class="container-fluid p-4 bg-light">
  <div class = "container">
    <h5 class="text-uppercase">Volunteer Connect</h5>
      <p>
        If you need support or want to see improvements to the site, feel free
        to message the developers! 
      </p>
      <p>
        <i class="fas fa-inbox me-3"></i>
        gdarley@tulane.edu
      </p>
    <p><i class="fas fa-map-marker-alt me-3"></i> 6823 St. Charles Ave, New Orleans, LA 70118</p>
  </div>

  </div>
  <!-- Grid container -->

</div>
  
</html>
