<!DOCTYPE html>
<html lang = "en">
    <head>
    <title>Organizations</title>
  </head>


<div class="container mt-3 p-5 my-5">
<h1>Organizations</h1>

<table class="table">
  <thead class="table-secondary">
    <tr>
      <th><%= link_to "Name", :sort => "name"%></th>
      <th><%= link_to "Organization Type", :sort => "category"%></th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organizations.each do |organization| %>
      <tr>
        <td><%= link_to "#{organization.name}", "#{organization.web_address}",:target => '_blank', style: 'color:rgb(0,0,0);', class: 'css_class' %></td>
        <td><%= organization.category %></td> 

        <td><%= link_to 'Show', organization, class:"btn btn-secondary btn-sm"  %></td>
        <td><%= link_to image_tag("icons8-pencil-24.png", size:"20"), edit_organization_path(organization), class:"btn btn-secondary btn-sm"  %></td>
        <td><%= button_to 'Delete', organization, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-secondary btn-sm"  %></td>
      </tr>
    <% end %>
  </tbody>
</table>
    
<style>
    tr {
    background-color:#02b300;
    color:white; }
    
  .button {
  background-color: lightblue; 
  border: solid black;
  color: black;
  padding: 15px 32px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
}
</style>
    
<br>

<%= link_to 'New Organization', new_organization_path, class:"button" %>
    
</div>
