<!DOCTYPE html>
<html lang = "en">
    <head>
    <title>Organizations</title>
  </head>


<div class="container mt-3 p-5 my-5">
<h1>Organizations</h1>

<table class="table">
  <thead class="table-secondary">
    <tr>
      <th>Name</th>
      <th>Organization Type</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organizations.each do |organization| %>
      <tr>
        <td><%= organization.name %></td>
        <td><%= organization.category %></td> 

        <td><%= link_to 'Show', organization, class:"btn btn-secondary btn-sm"  %></td>
        <td><%= link_to 'Edit', edit_organization_path(organization), class:"btn btn-secondary btn-sm"  %></td>
        <td><%= button_to 'Destroy', organization, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-secondary btn-sm"  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Organization', new_organization_path, class:"btn btn-secondary" %>
    
</div>
